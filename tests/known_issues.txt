# These are runnable scenes with known solver/runtime failures. Keeping this
# list separate makes the debt visible while ensuring every JSON is classified.
# Move a path into a run manifest as soon as it is repaired.

standard/homogenization.json
